{
  "name": "youtube-mirror",
  "private": true,
  "version": "0.0.1",
  "description": "Mirror a YouTube channel's videos, community posts, and comments to Bluesky via Cloudflare Workers",
  "type": "module",
  "scripts": {
    "build": "for c in wrangler.mirror-*.jsonc; do wrangler deploy --config $c --dry-run --outdir=dist || exit 1; done",
    "dev": "wrangler dev --config wrangler.mirror-channel.jsonc",
    "config:production": "tsx scripts/render-deploy-configs.ts --environment production",
    "config:ppe": "tsx scripts/render-deploy-configs.ts --environment ppe",
    "deploy:production": "tsx scripts/deploy-environment.ts --environment production",
    "deploy:ppe": "tsx scripts/deploy-environment.ts --environment ppe",
    "verify:production": "tsx scripts/verify-environment.ts --environment production --active-secrets --issuer",
    "verify:ppe": "tsx scripts/verify-environment.ts --environment ppe --active-secrets --issuer",
    "lint": "eslint . --quiet --max-warnings 0",
    "lint:fix": "eslint . --fix --quiet --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "cf-typegen:production": "npm run config:production -s && wrangler types --config .wrangler/deploy/production/wrangler.mirror-channel.jsonc",
    "cf-typegen:ppe": "npm run config:ppe -s && wrangler types --config .wrangler/deploy/ppe/wrangler.mirror-channel.jsonc",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "test:all": "npm run lint -s && npm run typecheck -s && npm run test -s && npm run build -s",
    "prepare": "node -e \"process.env.MSYS_NO_PATHCONV='1';import('husky').then(m=>process.stdout.write(m.default()))\" --input-type=module"
  },
  "dependencies": {
    "@atproto/api": "^0.19.3",
    "compromise": "^14.15.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.0",
    "@eslint/js": "^9.39.4",
    "@types/node": "^26.1.0",
    "eslint": "^9.32.0",
    "husky": "^9.1.7",
    "playwright": "^1.61.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.1",
    "vitest": "^4.0.18",
    "wrangler": "^4.73.0"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  }
}
